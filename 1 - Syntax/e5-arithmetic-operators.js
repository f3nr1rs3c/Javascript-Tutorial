let x = 5;
let y = 2;

/* Adding Operator */
let sum = x + y;
console.log("x:",x,"y:",y,"sum:",sum);

/* Subtraction Operator*/
let subtraction = x-y;
console.log("x:",x,"y:",y,"subtraction:",sum);

/* Multiplication Operator */
let multiplication = x * y;
console.log("x:",x,"y:",y,"multiplication:",multiplication);

/* Division Operator */
let division = x / y;
console.log("x:",x,"y:",y,"division:",division);

/* Modulus Operator */
let reaminder = x %  y;
console.log("x:",x,"y:",y,"reaminder:",reaminder);

/* Increment */
x++; // Sayıya 1 sayı daha ekliyor
y++;

console.log("x:",x,"y:",y);

/* Decrement*/
x--; // Sayıdan 1 çıkartıyor
y--;

console.log("x:",x,"y:",y);

// FİNİSH